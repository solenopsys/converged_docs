# Modulkatalog

Dieser Index wird aus der Converged-Modulregistrierung generiert. Jeder Eintrag verlinkt auf die von diesem Modul gepflegte Dokumentation; Abhängigkeiten stammen aus dessen Workspace-Paketmanifest, und die Lösungszugehörigkeit stammt aus `modules/solutions`.

## Zugriff und Sicherheit

### [lm-secrets](/en/docs/modules/lm-secrets)

Stellt den Dienstvertrag zum Speichern, Abrufen und Löschen benannter Secret-Werte bereit.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-access](/en/docs/modules/rp-access)

rp-access ist ein Repository in der sequrity-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [rp-auth](/en/docs/modules/rp-auth)

rp-auth ist ein Repository in der sequrity-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [rp-environment](/en/docs/modules/rp-environment)

Speichert und ruft Umgebungskonfigurationen ab, die Plattformbenutzern zugeordnet sind.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [rp-identity](/en/docs/modules/rp-identity)

rp-identity ist ein Repository in der sequrity-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [rp-oauth](/en/docs/modules/rp-oauth)

rp-oauth ist ein Repository in der sequrity-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-auth](/en/docs/modules/sf-auth)

sf-auth ist eine Surface in der sequrity-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [sf-secrets](/en/docs/modules/sf-secrets)

Stellt die Administrationsoberfläche zum Erstellen, Anzeigen, Aktualisieren und Löschen benannter Secret-Datensätze bereit.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-team](/en/docs/modules/sf-team)

sf-team ist eine Surface in der sequrity-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

## KI und Agenten

### [rp-assistant](/en/docs/modules/rp-assistant)

rp-assistant ist ein Repository in der ai-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`

### [rp-contexts](/en/docs/modules/rp-contexts)

Bietet Speicherung und Abruf benannter KI-Kontexte, einschließlich ihrer Sprachvarianten.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`

### [sf-assistants](/en/docs/modules/sf-assistants)

sf-assistants ist eine Surface in der ai-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: `sf-requests`
- Lösungen: `ai`

### [sf-contexts](/en/docs/modules/sf-contexts)

Bietet den KI-Arbeitsbereich zum Auflisten, Bearbeiten und Speichern benannter Kontexte in mehreren Sprachen.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`

## Analytik und Telemetrie

### [rp-counters](/en/docs/modules/rp-counters)

Speichert mandantenspezifische externe Analyse-Counter-Konfigurationen (Tracking-IDs, Head-Snippets) für die SSR-Injektion.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [rp-dashboard](/en/docs/modules/rp-dashboard)

Speichert persönliche Dashboard-Indikator-Pins: welche Widgets ein Benutzer angepinnt hat, deren Reihenfolge und Anzeigemetadaten.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [rp-logs](/en/docs/modules/rp-logs)

rp-logs ist ein Repository in der analytics-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [rp-telemetry](/en/docs/modules/rp-telemetry)

rp-telemetry ist ein Repository in der analytics-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [rp-usage](/en/docs/modules/rp-usage)

rp-usage ist ein Repository in der analytics-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [sf-dasboards](/en/docs/modules/sf-dasboards)

sf-dasboards ist eine Surface in der analytics-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [sf-logs](/en/docs/modules/sf-logs)

sf-logs ist eine Surface in der analytics-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [sf-telemetry](/en/docs/modules/sf-telemetry)

sf-telemetry ist eine Surface in der analytics-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

### [sf-usage](/en/docs/modules/sf-usage)

sf-usage ist eine Surface in der analytics-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `analitycs`

## Automatisierung und Orchestrierung

### [lm-kubernetes](/en/docs/modules/lm-kubernetes)

Integriert die Plattformautomatisierung über einen dedizierten Client und Dienstvertrag mit Kubernetes-Ressourcen.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-dag](/en/docs/modules/rp-dag)

rp-dag ist ein Repository in der automation-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `automation`, `requests`

### [rp-sheduller](/en/docs/modules/rp-sheduller)

rp-sheduller ist ein Repository in der automation-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `automation`

### [rp-webhooks](/en/docs/modules/rp-webhooks)

rp-webhooks ist ein Repository in der automation-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `automation`

### [sf-automation](/en/docs/modules/sf-automation)

sf-automation ist eine Surface in der automation-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `automation`

## Geschäftsdomäne

### [rp-billing](/en/docs/modules/rp-billing)

rp-billing ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `billing`

### [rp-equipment](/en/docs/modules/rp-equipment)

rp-equipment ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [rp-events](/en/docs/modules/rp-events)

Bietet Erstellung, Speicherung und Abruf von Geschäftsereignissen.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [rp-finance](/en/docs/modules/rp-finance)

Bietet Finanzoperationen für Transaktionen, Periodenzusammenfassungen, Cashflow, Forderungen und Verbindlichkeiten.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-invoices](/en/docs/modules/rp-invoices)

rp-invoices ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `billing`

### [rp-metering](/en/docs/modules/rp-metering)

rp-metering ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `billing`

### [rp-orders](/en/docs/modules/rp-orders)

Stellt den Dienstvertrag zum Erstellen, Aktualisieren, Auflisten und Verfolgen von Geschäftsaufträgen bereit.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [rp-requests](/en/docs/modules/rp-requests)

rp-requests ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [rp-reviews](/en/docs/modules/rp-reviews)

rp-reviews ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [rp-sales](/en/docs/modules/rp-sales)

rp-sales ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-staff](/en/docs/modules/rp-staff)

rp-staff ist ein Repository in der business-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [sf-equipment](/en/docs/modules/sf-equipment)

sf-equipment ist eine Surface in der business-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [sf-orders](/en/docs/modules/sf-orders)

Bietet die Vertriebsoberfläche für Auftrags- und Anfragelisten, Auftragsdetails, Statusfilterung und operative Dashboards.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [sf-requests](/en/docs/modules/sf-requests)

sf-requests ist eine Surface in der business-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [sf-reviews](/en/docs/modules/sf-reviews)

sf-reviews ist eine Surface in der business-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

## Kommunikation

### [rp-calls](/en/docs/modules/rp-calls)

rp-calls ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`

### [rp-chats](/en/docs/modules/rp-chats)

rp-chats ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [rp-community](/en/docs/modules/rp-community)

rp-community ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [rp-notify](/en/docs/modules/rp-notify)

rp-notify ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [rp-resonus](/en/docs/modules/rp-resonus)

Bietet die Kommunikationskonfiguration für verwaltete Telefonnummern und LLM-Gate-Einstellungen.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-support](/en/docs/modules/rp-support)

rp-support ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [rp-threads](/en/docs/modules/rp-threads)

rp-threads ist ein Repository in der communications-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`, `communications`

### [sf-calls](/en/docs/modules/sf-calls)

sf-calls ist eine Surface in der communications-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`

### [sf-chats](/en/docs/modules/sf-chats)

sf-chats ist eine Surface in der communications-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [sf-community](/en/docs/modules/sf-community)

sf-community ist eine Surface in der communications-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [sf-support](/en/docs/modules/sf-support)

sf-support ist eine Surface in der communications-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`

### [sf-threads](/en/docs/modules/sf-threads)

sf-threads ist eine Surface in der communications-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `ai`, `communications`

## Inhalte und Dokumente

### [rp-classifier](/en/docs/modules/rp-classifier)

rp-classifier ist ein Repository in der content-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-galery](/en/docs/modules/rp-galery)

rp-galery ist ein Repository in der content-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `content`

### [rp-markdown](/en/docs/modules/rp-markdown)

rp-markdown ist ein Repository in der content-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `content`

### [rp-scripts](/en/docs/modules/rp-scripts)

Bietet Speichervorgänge für Skriptdateien, einschließlich Lesen, Speichern, Hashing und Löschen.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-static](/en/docs/modules/rp-static)

Stellt den Dienstvertrag für statische Inhalte und SSR-Cache-Metadaten bereit.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-struct](/en/docs/modules/rp-struct)

rp-struct ist ein Repository in der content-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `content`

### [sf-classifier](/en/docs/modules/sf-classifier)

Bietet die Klassifikator-Oberfläche zum Navigieren von Entitäten, Mappings und Baumstrukturen.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-docs](/en/docs/modules/sf-docs)

sf-docs ist eine Surface in der content-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-galery](/en/docs/modules/sf-galery)

sf-galery ist eine Surface in der content-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-landing](/en/docs/modules/sf-landing)

sf-landing ist eine Surface in der content-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-markdown](/en/docs/modules/sf-markdown)

sf-markdown ist eine Surface in der content-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-static](/en/docs/modules/sf-static)

Bietet die Operationsoberfläche zum Prüfen und Leeren statischer SSR-Cache-Einträge.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-struct](/en/docs/modules/sf-struct)

sf-struct ist eine Surface in der content-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

## Dateien und Speicher

### [lm-compressors](/en/docs/modules/lm-compressors)

lm-compressors ist eine Lambda in der data-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [rp-dumps](/en/docs/modules/rp-dumps)

rp-dumps ist ein Repository in der data-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [rp-files](/en/docs/modules/rp-files)

rp-files ist ein Repository in der data-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `communications`, `requests`

### [rp-store](/en/docs/modules/rp-store)

rp-store ist ein Repository in der data-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [sf-dumps](/en/docs/modules/sf-dumps)

sf-dumps ist eine Surface in der data-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [sf-files](/en/docs/modules/sf-files)

sf-files ist eine Surface in der data-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

## Anbieter für Nachrichtenzustellung

### [lm-lemonsqueezy](/en/docs/modules/lm-lemonsqueezy)

lm-lemonsqueezy ist eine Lambda in der providers-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `billing`

### [lm-push](/en/docs/modules/lm-push)

lm-push ist eine Lambda in der providers-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [lm-ses](/en/docs/modules/lm-ses)

lm-ses ist eine Lambda in der providers-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `security`

### [lm-sms](/en/docs/modules/lm-sms)

lm-sms ist eine Lambda in der providers-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [lm-smtp](/en/docs/modules/lm-smtp)

lm-smtp ist eine Lambda in der providers-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

## Modellkonvertierung

### [lm-modelconvertor](/en/docs/modules/lm-modelconvertor)

lm-modelconvertor ist eine Lambda in der convertors-Domäne. Ihr detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

## Workflows

### [wf-dialogue-summary](/en/docs/modules/wf-dialogue-summary)

Fasst unverarbeitete Chat- und Anrufdialoge mit einem LLM zusammen und speichert anschließend Titel, Beschreibungen und Lärmklassifizierung.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [wf-equipment-incident](/en/docs/modules/wf-equipment-incident)

wf-equipment-incident ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [wf-file-analyze](/en/docs/modules/wf-file-analyze)

Analysiert eine gespeicherte Nicht-Archivdatei und erstellt Modellvorschauen sowie CNC- oder 3D-Druck-Kalkulationen, sofern unterstützt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [wf-file-unpack](/en/docs/modules/wf-file-unpack)

Expandiert ein hochgeladenes Archiv in eine Sammlung gespeicherter Dateien zur anschließenden Analyse.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [wf-files-analyze](/en/docs/modules/wf-files-analyze)

wf-files-analyze ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [wf-files-process](/en/docs/modules/wf-files-process)

Verarbeitet hochgeladene Dateien in Stapeln: Es expandiert Archive, identifiziert Modelldateien und erstellt daraus eine Fertigungsanfrage.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [wf-order-review-followup](/en/docs/modules/wf-order-review-followup)

wf-order-review-followup ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [wf-order-review-request](/en/docs/modules/wf-order-review-request)

wf-order-review-request ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [wf-payment-settle](/en/docs/modules/wf-payment-settle)

wf-payment-settle ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `billing`

### [wf-request-analyze](/en/docs/modules/wf-request-analyze)

wf-request-analyze ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `requests`

### [wf-request-to-order](/en/docs/modules/wf-request-to-order)

wf-request-to-order ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

### [wf-sales-import](/en/docs/modules/wf-sales-import)

wf-sales-import ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [wf-sales-review-outreach](/en/docs/modules/wf-sales-review-outreach)

wf-sales-review-outreach ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: keine

### [wf-team-invite](/en/docs/modules/wf-team-invite)

wf-team-invite ist ein Workflow in der Plattform-Domäne. Sein detaillierter Zweck wird mit der Modulquelle gepflegt.

- Direkte Abhängigkeiten: keine
- Lösungen: `production`

## Lösungsabhängigkeiten

- `ai`: `security`
- `analitycs`: `security`
- `automation`: `security`
- `billing`: `security`
- `communications`: `security`
- `content`: `security`
- `production`: `security`, `analitycs`, `requests`
- `requests`: keine Lösungsabhängigkeiten
- `security`: keine Lösungsabhängigkeiten
